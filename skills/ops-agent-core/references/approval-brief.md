# 9 要素审批简报

任何 write/exec（以及 GuidedManual 中建议用户点击的危险 IDE 命令）必须包含：

1. 目标与理由
2. 支持证据（引用 EvidenceNote id）
3. 预期影响与中断
4. 前置检查
5. 备份方式与位置
6. 确切命令 / 文件操作（将计算 `commandSetSha256`）
7. 成功判据
8. 回滚触发与确切步骤
9. 剩余不确定性

用户必须通过 ApprovalBar 明确批准。目标/命令/影响/回滚变化则令牌作废。
