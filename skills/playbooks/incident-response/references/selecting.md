# Selecting · 故障排查

默认 select：`{mode: replace, pluginIds: [at.grafana]}`（Orchestrator 代发）。

允许：确认已选 provider；需要主机 / 构建 / SQL 证据时**一次** add
（at.jumpserver 或 at.terminal 二选一主机面，不混短名；构建面 at.jenkins）。
禁止：二次 replace；调查中 clear；一次选入全部插件。

DoD：一轮 select 完成、工具面覆盖当前假设 → Investigating。
