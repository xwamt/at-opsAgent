# AwaitingApproval · 故障排查

允许：产出并修订 9 要素审批简报（模板：ops-agent-core/references/
approval-brief.md）；回答用户对简报的追问，只读取证可继续。
禁止：未批准先动手；把 IDE 弹窗当批准。

简报实质变化（目标 / 命令 / 影响 / 回滚）→ 令牌作废，重新审批。

DoD：ApprovalBar 明确批准（签发 approvalToken）→ Executing；
拒绝或只要方案 → Reporting。
