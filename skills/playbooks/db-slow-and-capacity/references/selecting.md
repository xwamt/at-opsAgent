# Selecting · 数据库慢查询与容量

默认 select：`{mode: replace, pluginIds: [at.jumpserver]}`（MySQL/Redis 经
堡垒机；exec 需已连接 IDE 终端，先 `jumpserver_get_terminal_context`）。
指标面需要时一次 add（at.grafana 或 at.database——注意 at.database 的
write 无插件弹窗，强制会话审批）。
引用上限：每假设 1 provider 附录 + 1 ops reference。
