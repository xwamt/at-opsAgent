# Synthesizing · 发布与回滚

产出发布 / 回滚说明草案：目标与精确 digest、变更集、预期中断、
健康门与中止阈值、回滚制品与步骤、不可逆项。

出口：触发构建属只读插件域 → GuidedManual（先出 9 要素级说明，再给深链）。
主机侧 exec（如 kubectl rollout undo）→ 按通用审批纪律出 9 要素简报，
会话批准后才执行。

禁止：跳过说明直接给深链按钮。
